# Errors only. Pyflakes catches unused imports, undefined names and shadowed
# builtins; E4/E7/E9 catch import, statement and syntax faults. Style rules are
# left off on purpose - a lint job that fails on line length trains you to
# ignore it.
line-length = 100

# archived single-file version, kept for reference and not maintained
exclude = ["old-models"]

[lint]
select = ["F", "E4", "E7", "E9"]

[lint.per-file-ignores]
"run_comparison.py" = ["E402"]   # matplotlib.use("Agg") must precede pyplot
